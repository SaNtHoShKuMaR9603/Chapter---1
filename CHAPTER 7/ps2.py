# List of names
l = ["Harry", "Soham", "Sachin ", "Rahul"]

# Iterate through each name in the list
for name in l:
    # Check if the name starts with 'S'
    if name.startswith("S"):
        # Print greeting message for names starting with 'S'
        print("Hello", name, "\nGood Morning!")